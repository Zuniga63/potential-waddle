export interface SheetTown {
  id: string;
  name: string;
  description?: string;
  departmentNumber?: number;
  enabled?: boolean;
  longitude?: string;
  latitude?: string;
  urbanArea?: number;
}
